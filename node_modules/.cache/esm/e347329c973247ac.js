let express,mongoose,APP_PORT,DB_URL,errorHandler,routes;_973‍.w("express",[["default",["express"],function(v){express=v}]]);_973‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_973‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}],["DB_URL",["DB_URL"],function(v){DB_URL=v}]]);_973‍.w("./middlewares/errorHandler",[["default",["errorHandler"],function(v){errorHandler=v}]]);_973‍.w("./routes",[["default",["routes"],function(v){routes=v}]]);

var bodyParser = require('body-parser');



const app = express()


// DATABASE CONNECTION
mongoose.connect(DB_URL, {
    
 });
const db = mongoose.connection;
db.on('error',_973‍.g.console.error.bind(_973‍.g.console,'connection error:'))
db.once('open', () => {
    console.log('DB connected...');
});

app.use(express.json());
app.use('/api', routes);

app.use(bodyParser.json());
// configure the app to use bodyParser()
app.use(bodyParser.urlencoded({
    extended: true
}));
app.use(errorHandler);
app.listen(APP_PORT, () => console.log(`listening on porting ${APP_PORT}`))