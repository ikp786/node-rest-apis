let DEBUG_MODE,ValidationError,CustomErrorHandler;_b16‍.x([["default",()=>_b16‍.o]]);_b16‍.w("../config",[["DEBUG_MODE",["DEBUG_MODE"],function(v){DEBUG_MODE=v}]]);_b16‍.w("joi",[["ValidationError",["ValidationError"],function(v){ValidationError=v}]]);_b16‍.w("../services/CustomErrirHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);


const errorHandler = (err, req, res, next) => {
    let statsCode = 500;
    let data = {
        message: "Iternal Server error",
        ...(DEBUG_MODE === 'true' && { originalError: err.message })
    }

    if (err instanceof ValidationError) {
        statsCode = 422;
        data = {
            message: err.message
        }
    }
    if(err instanceof CustomErrorHandler){
        statsCode = err.status;
        data  = {
            message: err.message
        }
    }

    return res.status(statsCode).json(data);

}


_b16‍.d(errorHandler);